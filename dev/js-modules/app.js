(function () {

    let app = angular.module('app', []);

    /*function compileCards() {

        var data = {};
        data['cards'] = cards;
        var templateScript = $('#cards').html();
        var template = Handlebars.compile(templateScript);
        $('.cards-list').append(template(data));
    }

    return {
        init: function() {
            compileCards();
        }
    }*/
})();



//app.init();